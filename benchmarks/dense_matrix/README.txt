
Any output line to the benchmarks should produce a boolean 'true'
plus the rest of the timing lines.
The examples are testing if the code is correct or not,
and in parallel demonstrate the use of containers.